# Outputs

Generated rasters and large figures are intentionally excluded from the source repository.

The master notebook creates lightweight tables/manifests and larger local scientific products.
For archival release, publish only outputs permitted by provider licensing and repository size limits.
The notebook's SHA-256 manifest identifies the exact frozen local artifacts.
